name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hi!
        run: echo "Hello World!"
      - run: ls -al
      - uses: actions/checkout@v3.5.3
      - run: ls -al
